import { NumericBinValue } from "./constant/constant";
export declare class NumericBin {
    /**
     * This func will return the array of bin from (with, unit)
     *
     * @param min
     * @param max
     * @param width
     * @param nice
     */
    makeUniformBins(min: number, max: number, width: number, nice: boolean): NumericBinValue[];
    /**
     * This function will return an array of bin which is closest with your expected
     *
     * @param min
     * @param max
     * @param expected
     * @param nice
     */
    makeAutoBins(min: number, max: number, expected: number, nice: boolean): NumericBinValue[];
    private suggestWidth;
    private findClosetIndex;
}
