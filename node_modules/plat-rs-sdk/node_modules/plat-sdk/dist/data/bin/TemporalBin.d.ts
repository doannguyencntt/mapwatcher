import { TemporalBinValue } from "./constant/constant";
export declare class TemporalBin {
    /**
     * This func will return the array of bin from (width, unit)
     *
     * @param min - start date,
     * @param max - end date,
     * @param unit - 'Y','Q','M','W','d','m',
     * @param width - integer and greater then 0
     */
    makeUniformBins(min: Date, max: Date, unit: string, width: number, maxBinAmount?: number): TemporalBinValue[];
    private formatLabel;
    /**
     * This func will return the array of bin which have amount closest your expected
     *
     * @param min - min Date
     * @param max - max Date
     * @param expected - integer and greater then 0
     */
    makeAutoBins(min: any, max: any, expected: number): TemporalBinValue[];
    private getAmountBinSuggest;
    private findClosetIndex;
}
